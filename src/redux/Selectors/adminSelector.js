export const isLoadingSelector = (state) => state.admin.isLoading;

export const userListSelector = (state) => state.admin.data;

export const isSuccessSelector = (state) => state.admin.isSuccess;

export const messageAdminAddAccountSelector = (state) => state.admin.message;
