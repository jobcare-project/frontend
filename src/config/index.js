import routes from './routes';
import {
    authApiUrl,
    jobsApiUrl,
    uploadApiUrl,
    usersApiUrl,
    searchApiUrl,
    chartApiUrl,
} from './apis';

const config = {
    routes,
    authApiUrl,
    jobsApiUrl,
    uploadApiUrl,
    usersApiUrl,
    searchApiUrl,
    chartApiUrl,
};

export default config;
