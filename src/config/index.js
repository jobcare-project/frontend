import routes from './routes';
import {
    authApiUrl,
    jobsApiUrl,
    uploadApiUrl,
    usersApiUrl,
    searchApiUrl,
    chartApiUrl,
    candidatesApiUrl,
} from './apis';

const config = {
    routes,
    authApiUrl,
    jobsApiUrl,
    uploadApiUrl,
    usersApiUrl,
    searchApiUrl,
    chartApiUrl,
    candidatesApiUrl,
};

export default config;
