import routes from './routes';
import {
    authApiUrl,
    jobsApiUrl,
    uploadApiUrl,
    usersApiUrl,
    searchApiUrl,
} from './apis';

const config = {
    routes,
    authApiUrl,
    jobsApiUrl,
    uploadApiUrl,
    usersApiUrl,
    searchApiUrl,
};

export default config;
