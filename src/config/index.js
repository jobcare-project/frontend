import routes from './routes';
import { authApiUrl, jobsApiUrl, uploadApiUrl } from './apis';

const config = {
    routes,
    authApiUrl,
    jobsApiUrl,
    uploadApiUrl,
};

export default config;
